name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build-test:
    uses: fynxlabs/github-workflows/.github/workflows/zig-ci.yml@master
    with:
      zig-fmt-paths: "zig/"
